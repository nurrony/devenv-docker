version: '2'
services:
  mariadb:
    image: k0st/alpine-mariadb
    container_name: mariadb
    restart: always
    volumes:
      - /media/devzone/projects/storage/mysql-common:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=nmrony
